// This file was empty and caused Expo Router to warn about a missing default export.
// Keeping a minimal placeholder component to satisfy the router.
// If you intended to store React context, move it outside the `app/` folder (e.g., `src/context/`).

import React from 'react';

export default function AuthContextPlaceholder() {
	return null;
}

